# GITHUB_TOKEN is intentionally NOT loaded from this file. The implementer
# agent runs in a sibling worktree with sandbox bypass and can read anything
# under REPO_ROOT, so a PAT here would be recoverable via a prompt injection.
#
# Provide the token via one of:
#   * Export GITHUB_TOKEN in the orchestrator's launch environment, or
#   * Write it to ~/.config/geserdugarov/agent-orchestrator/token (chmod 600;
#     path is derived from REPO).
# Override the file path with ORCHESTRATOR_TOKEN_FILE if you want a different
# location -- pick a path the agent worktree cannot reach via known relatives.
#
# Fine-grained PAT scopes: read/write on Contents, Issues, Pull requests,
# Metadata. Add Checks: Read if you plan to enable AUTO_MERGE -- without it,
# Actions-only PRs report check_state='none' and park indefinitely waiting
# for a human, even when CI is green. Create the PAT at
# https://github.com/settings/personal-access-tokens

# owner/name of the repo the orchestrator is responsible for. Used as the
# legacy single-repo configuration; when REPOS is set (see below) this and
# the matching TARGET_REPO_ROOT / BASE_BRANCH are ignored.
REPO=geserdugarov/agent-orchestrator

# Path to the local clone of REPO. Worktrees are `git worktree add`-ed from
# here, so commits land on this clone's history. Defaults to REPO_ROOT (the
# orchestrator's own checkout) for the self-bootstrap case. Set this when the
# orchestrator code lives in one clone but drives PRs into a different repo.
# TARGET_REPO_ROOT=/path/to/target/clone

# Base branch in the *target* repo. Used for both the worktree base and the
# PR base. Default `main`; set to whatever the target's default branch is
# (e.g. `master`).
# BASE_BRANCH=main

# Name of the git remote in TARGET_REPO_ROOT that points at REPO on GitHub.
# Defaults to `origin`. Override when the local clone tracks several remotes
# (e.g. a public `origin` and a private fork named `private`) and the
# orchestrator should drive the non-default one. Ignored when REPOS is set --
# put the remote name in the per-entry fourth field there instead.
# REMOTE_NAME=origin

# Multi-repo configuration. Each entry is `owner/name|target_root|base_branch`
# with OPTIONAL fourth `|remote_name` (defaults to `origin`) and fifth
# `|parallel_limit` (defaults to MAX_PARALLEL_ISSUES_PER_REPO) fields,
# separated by a newline OR a `;` (the .env loader cannot represent newlines
# inside a value, so use `;` here and prefer newlines when exporting in a
# shell). The fifth field is positional: to override `parallel_limit` you
# must also write the `remote_name` (use `origin` explicitly to keep the
# default).
#
# When REPOS is set the legacy REPO/TARGET_REPO_ROOT/BASE_BRANCH/REMOTE_NAME
# quartet is ignored and the orchestrator polls every entry on each tick.
# Worktrees live under `WORKTREES_DIR/<owner>__<name>/issue-N` so two repos
# with the same issue number cannot collide.
#
# Each repo needs its own GitHub token at `~/.config/<owner>/<repo>/token`
# (or set GITHUB_TOKEN once in the environment if a single PAT has access to
# every listed repo). Validation happens at import: malformed entries, empty
# owner/name or base_branch, duplicate slugs, and non-positive-integer
# parallel_limits all abort startup.
# REPOS=owner1/repo1|/path/to/repo1|main;owner2/repo2|/path/to/repo2|master|private|2

# Where per-issue git worktrees are created. The orchestrator namespaces each
# worktree by repo slug (so two repos with the same issue number cannot share
# a checkout), giving the on-disk layout `WORKTREES_DIR/<owner>__<name>/issue-N`
# (and `decompose-N` for the read-only decomposer scratch).
# WORKTREES_DIR=../wt-orchestrator

# Base branch of the orchestrator's *own* repo (REPO_ROOT). Used only by the
# self-update path: run.sh fast-forwards to it on every restart, and
# `_self_modifying_merge_happened` watches `origin/<this>` for new commits
# that touch `orchestrator/`. Decoupled from BASE_BRANCH so the target repo
# can have a different default branch without breaking self-update.
# ORCHESTRATOR_BASE_BRANCH=main

# Polling interval for the main loop, seconds.
POLL_INTERVAL=60

# Hard wall-clock cap per agent invocation, seconds.
AGENT_TIMEOUT=1800

# Hard wall-clock cap for the codex review pass, seconds. Defaults to AGENT_TIMEOUT.
REVIEW_TIMEOUT=1800

# How many review/fix iterations to attempt before parking on awaiting_human.
MAX_REVIEW_ROUNDS=10

# How many auto-conflict-resolution rounds `_handle_resolving_conflict` runs
# before parking awaiting_human. Each round merges origin/<BASE_BRANCH> into
# the per-issue branch and resumes the dev agent on remaining conflicts.
MAX_CONFLICT_ROUNDS=10

# Cap on how many fresh implementing-codex spawns one issue can consume within
# a 24h window opened at the first counted attempt; the window resets once 24h
# has elapsed. Resumes on human reply do NOT count against the budget.
# 0 means unbounded.
MAX_RETRIES_PER_DAY=10

# Per-repo cap on how many issues the orchestrator may advance in parallel
# within one repo on a single tick. Default 1 keeps the legacy "one issue at
# a time per repo" behavior. Each REPOS entry can override this via its
# optional fifth pipe-separated field. Must be a positive integer; non-numeric
# or non-positive values abort startup.
# MAX_PARALLEL_ISSUES_PER_REPO=1

# Global cap across all configured repos. Default 3 bounds concurrent agent
# spawn fan-out regardless of any one repo's per-repo cap; raise this only on
# hosts with the CPU / memory headroom to run more agent CLIs at once. Must
# be a positive integer.
# MAX_PARALLEL_ISSUES_GLOBAL=3

# Comma-separated GitHub logins the orchestrator @-mentions when it needs human attention.
HITL_HANDLE=github-user

# Comma-separated GitHub logins whose unlabeled issues the orchestrator is
# allowed to auto-pick-up. Empty (default) disables the allowlist and
# preserves the legacy "anyone can trigger" behavior. Set this on a public
# repo so random users can't spend agent compute on useless tasks. The guard
# only applies at pickup; if a maintainer hand-labels an outsider's issue
# (e.g. `implementing`), the workflow still drives it to completion.
# ALLOWED_ISSUE_AUTHORS=github-user,agent-orchestrator

# Which backend implements vs. reviews vs. decomposes. Each value is a
# shell-like command spec: the FIRST TOKEN names the backend and must
# match `codex` or `claude` case-insensitively (`CODEX`, `Claude`, and
# `codex` all parse to the same backend; the lowercased form is used
# only for dispatch in agents.py -- pinned state stores the raw spec
# verbatim, so `DEV_AGENT=CODEX -m gpt-5.5` is persisted as the literal
# `CODEX -m gpt-5.5` and the lowercase happens again every resume when
# `_parse_agent_spec` re-parses it). Anything else aborts at startup
# so a typo cannot silently fall back.
#
# Any remaining tokens are forwarded verbatim as backend-CLI args on
# every spawn for that role -- typically model and reasoning-effort
# selection. Quoting follows shell rules (parsed with `shlex.split`).
#
# Examples:
#   DEV_AGENT=claude
#   DEV_AGENT=claude --model claude-opus-4-7
#   DEV_AGENT=claude --model claude-opus-4-7 --effort high
#   DEV_AGENT=codex -m gpt-5.5 -c 'model_reasoning_effort="xhigh"'
#   REVIEW_AGENT=codex -m gpt-5.5-codex
#   REVIEW_AGENT=codex -m gpt-5.5-codex -c 'model_reasoning_effort="high"'
#   REVIEW_AGENT=claude --model claude-sonnet-4-6 --effort high
#   DECOMPOSE_AGENT=claude --model claude-opus-4-7 --effort medium
#
# Interaction with CODEX_BIN / CLAUDE_BIN: the first token is only a
# backend selector (it picks the codex vs. claude runner in agents.py).
# The actual executable launched is `CODEX_BIN` when the first token is
# `codex` and `CLAUDE_BIN` when it is `claude`, so set those to a full
# path when the CLI is not on `$PATH`. Writing the full path as the first
# token (e.g. `DEV_AGENT=/usr/local/bin/codex ...`) is rejected.
#
# In-flight issues keep using the pinned full spec until the agent
# session ends: the spec the spawn ran with (backend AND args) is
# persisted to the issue's pinned state (`dev_agent` /
# `decomposer_agent` / `question_agent`) before the spawn, and
# `_handle_*` re-parses that stored spec on every resume. Flipping
# `DEV_AGENT` / `DECOMPOSE_AGENT` in env therefore only affects fresh
# issues; any issue with a live session keeps the original backend +
# args until it reaches a terminal label (`done` / `rejected`).
#
# DECOMPOSE_AGENT also drives the `question` workflow stage (read-only
# Q&A on the `question` label, no PR opened, no branch pushed). The
# first question spawn for an issue seeds `question_agent` from
# DECOMPOSE_AGENT, and subsequent human replies resume that locked
# spec via `_handle_question` -- so an env flip cannot retarget an
# in-flight Q&A at a different CLI either. See docs/workflow.md for
# the full question-stage contract.
#
# The reviewer is spawned fresh every round, so `REVIEW_AGENT` changes
# take effect on the next round; the most recent value is recorded in
# `review_agent` for traceability. DECOMPOSE_AGENT is validated at
# import even when DECOMPOSE=off, so toggling DECOMPOSE back on never
# surfaces a fresh "that env var was always invalid" failure.
DEV_AGENT=claude
REVIEW_AGENT=codex
DECOMPOSE_AGENT=claude

# Path to the codex CLI binary -- the executable launched when a role's
# first token is `codex`. Override only if not on $PATH.
CODEX_BIN=codex

# Path to the claude CLI binary -- the executable launched when a role's
# first token is `claude`. Override only if not on $PATH.
CLAUDE_BIN=claude

# git identity injected into each codex spawn via GIT_AUTHOR_*/GIT_COMMITTER_*
# env vars, so agent commits are attributable to the orchestrator rather than
# the host's git config. The default email uses GitHub's noreply form so it
# won't bounce and won't link to any real user account.
# AGENT_GIT_NAME=agent-orchestrator
# AGENT_GIT_EMAIL=agent-orchestrator@users.noreply.github.com

# Allow the orchestrator to merge approved + mergeable + green-CI PRs out of
# `in_review`, transitioning the issue to `done` and closing it. Default off
# so existing setups keep the manual-merge contract; flip to `on` once the
# loop is dogfooded. Truthy spellings: 1, true, on, yes.
#
# With AUTO_MERGE off the orchestrator instead pings every HITL_HANDLE once
# per PR head SHA when the PR becomes mergeable, so humans know the PR is
# ready for review/merge without polling the issue list manually.
# AUTO_MERGE=off

# Quiet window (seconds) after the most recent PR/issue comment in `in_review`
# before resuming the dev session on the comment. Matches the 10-minute
# debounce documented in docs/workflow.md. Lower values risk firing while a
# human is still typing; higher values delay reaction to PR feedback.
# IN_REVIEW_DEBOUNCE_SECONDS=600

# Kill switch for the `decomposing` stage. on (default) routes new unlabeled
# issues through `_handle_decomposing` which asks DECOMPOSE_AGENT for a
# fenced `orchestrator-manifest` JSON block (decision: single | split). off
# reverts to the legacy direct-to-`implementing` pickup -- useful as a
# rollout safety valve if manifest output proves unreliable.
#
# The flag also applies mid-flight: if an issue was already labeled
# `decomposing` (or parked there awaiting a human) when the orchestrator
# restarts with the flag off, the next tick routes it to `implementing` too
# instead of resuming the decomposer. Half-finished splits already on GitHub
# still finalize to `blocked` -- the kill switch must not strand orphan
# child issues.
#
# Truthy spellings: 1, true, on, yes; anything else (including typos)
# disables it.
# DECOMPOSE=on

# Local verification commands run in the per-issue worktree when the
# reviewer agent emits `VERDICT: APPROVED`, before the issue is labeled
# `in_review`. Default empty (no verification, preserving legacy
# behavior). Each entry is a single shell command; separate entries with
# `;` (the .env loader cannot represent newlines inside a value, so use
# `;` here and prefer newlines when exporting in a shell). Blank lines
# and lines starting with `#` are skipped. Commands run sequentially via
# the shell so quoting / pipes / `&&` work the way an operator would
# type them; stdout and stderr are merged into one captured block.
#
# On a non-zero exit, a per-command timeout, a dirty worktree left
# behind, or a command that moved HEAD (e.g. its own `git commit`), the
# issue is parked in `validating` with `park_reason` set to one of
# `verify_failed`, `verify_timeout`, `verify_dirty`, or
# `verify_head_changed`; the park comment names the failing command, its
# exit code (or timeout), and a redacted / truncated tail of the
# captured output. The approval comment, squash, watermark seeding, and
# `in_review` handoff only fire on a clean run. CI remains the later
# auto-merge gate -- the verify gate is the first gate, not the only
# one.
#
# Examples:
#   VERIFY_COMMANDS=python3 -m pytest -q
#   VERIFY_COMMANDS=python3 -m pytest -q;ruff check .
# VERIFY_COMMANDS=

# Per-command wall-clock cap in seconds for VERIFY_COMMANDS. A single
# slow command parks the issue with `verify_timeout` rather than burning
# the orchestrator's tick budget. Default 600 (10 min). Ignored when
# VERIFY_COMMANDS is empty.
# VERIFY_TIMEOUT=600

# Optional JSONL event sink. When set, the orchestrator appends one JSON
# object per audit event -- `{ts, repo, issue, event, stage, ...}`, UTC
# timestamps at second precision, keys sorted -- to the path below.
#
# Event kinds emitted today: `stage_enter`, `agent_spawn` / `agent_exit`,
# `review_verdict`, `park_awaiting_human`, `pr_opened`, `pr_merged`,
# `pr_closed_without_merge`, `merge_attempt`, `conflict_round`. See the
# "Audit event log" section in docs/architecture.md for the full schema,
# per-kind emitter, and the extras each record carries.
#
# Tail this file to follow the workflow without scraping logs. Unset
# (the default) keeps the legacy behavior: no file is opened. Synchronous
# append; create the parent directory if it does not exist. No built-in
# rotation -- pair with `logrotate` (or your platform's equivalent) if you
# leave the orchestrator running for long enough that file size matters.
#
# Precedence: the pinned JSON state comment on each issue remains the
# authoritative per-issue state. The event log is append-only and exists
# for audit / observability only; if it disagrees with pinned state (e.g.
# a write to disk failed and was logged-and-swallowed, the file was
# truncated by rotation, or events were lost to a disk-full window),
# trust pinned state and treat the log as a lossy tail.
# EVENT_LOG_PATH=/var/log/orchestrator/events.jsonl

# Project-local analytics sink. Distinct from EVENT_LOG_PATH (the audit
# event log driven by GitHubClient.emit_event): this is a foundation
# layer for future aggregation / reporting work and is opted in / out
# independently. `orchestrator/analytics/` appends one JSON object per
# line `{ts, repo, issue, event, optional stage, ...}` (UTC ISO-8601
# timestamps at second precision, keys sorted). The raw JSONL is the
# foundation layer: it is intended to be ingested later into a
# structured database (SQLite / DuckDB / Postgres) for aggregation and
# reporting; one record per line keeps the ingestion path streaming.
# `analytics.prune_old_records(...)` removes records older than
# ANALYTICS_RETENTION_DAYS.
#
# Settings ownership: ANALYTICS_LOG_PATH, ANALYTICS_RETENTION_DAYS,
# and ANALYTICS_DB_URL (further below) are parsed at import inside
# `orchestrator/analytics/__init__.py` -- not in `orchestrator/config.py`
# -- so the analytics package owns its own configuration surface and
# consumers of `config.LOG_DIR` do not pull analytics defaults in
# transitively. EVENT_LOG_PATH above intentionally stays in `config`
# because the audit event log is a general-purpose surface.
#
# Event kinds written today:
#   * stage_enter      - one record per workflow label transition,
#                        emitted by GitHubClient._emit_stage_enter
#                        alongside the EVENT_LOG_PATH audit event.
#   * stage_evaluation - one record per `_process_issue` dispatch,
#                        carrying stage / duration_s / result
#                        (`ok` or `error`). Non-agent stages get the
#                        same timing context as the agent-driven ones.
#   * agent_exit       - one record per tracked agent invocation,
#                        carrying token / model / cost details from
#                        `usage.parse_agent_usage` (see workflow.py's
#                        `_run_agent_tracked` for the field list).
#
# Default path: <LOG_DIR>/analytics.jsonl (already covered by the
# `logs/` .gitignore rule). Set ANALYTICS_LOG_PATH= (empty) or to one
# of `off` / `disabled` / `none` to disable writes entirely -- in that
# mode the helpers are silent no-ops and no file is opened.
#
# Filesystem / JSONL only. No PostgreSQL, Streamlit, or external
# services. Pinned GitHub state on each issue remains authoritative;
# the analytics file is local-filesystem observability and is safe to
# truncate or delete at any time without affecting workflow
# correctness.
# ANALYTICS_LOG_PATH=

# Retention window for ANALYTICS_LOG_PATH in days. Records whose `ts`
# is older than this window are removed by
# `analytics.prune_old_records(...)`. The polling loop calls the
# prune helper once per tick (after every configured repo drains) so
# retention is actually applied without operator intervention. Pruning
# never touches pinned GitHub state on any issue; a runaway error
# inside the prune is logged and swallowed so analytics
# misconfiguration cannot stop the poll loop. Default 90 days. Set
# to 0 (or any non-positive value) to keep raw data indefinitely --
# the prune helper becomes a no-op so operators can opt out of
# cleanup without disabling the sink itself.
# ANALYTICS_RETENTION_DAYS=90

# Connection string for the analytics Postgres service defined in
# `analytics-db/compose.yml`. Consumed by `orchestrator.analytics.sync`,
# an operator-driven CLI that replays JSONL records from
# ANALYTICS_LOG_PATH into the database. Not read by the polling loop --
# the orchestrator's tick correctness does not depend on database
# availability, so leaving this unset costs nothing.
#
# The example below matches the compose defaults and a same-host
# deployment. To move the database off-host later, repoint this URL
# (and override the compose-side POSTGRES_* in `analytics-db/.env`, or
# stop running the compose service entirely). The endpoint is
# deliberately a single libpq URL so swapping local for remote -- or
# unix-socket for TCP -- is a one-line config change.
#
# Empty value and the sentinels `off` / `disabled` / `none` (case-
# insensitive) disable the sync the same way ANALYTICS_LOG_PATH does,
# so the two can be turned off in parallel.
#
# Run the sync on demand:
#   uv run python -m orchestrator.analytics.sync
#
# Each row is deduplicated by the SHA-256 of the canonical JSON form
# of its source record (the `content_hash` column added by
# `analytics-db/init/01-schema.sql`), so repeated runs are idempotent
# even after `analytics.prune_old_records` rewrites the JSONL file.
# Malformed lines (blank, non-JSON, non-object, missing required
# `ts` / `repo` / `issue` / `event` keys) are logged and counted but
# never abort the sync; the JSONL file is treated as read-only.
#
# An ALTER TABLE / CREATE UNIQUE INDEX guard in the init script
# adds the `content_hash` column idempotently so an operator who
# already deployed the compose service before this column existed
# can migrate without dropping the data volume. Re-run the mounted
# init script against the running compose service:
#   cd analytics-db
#   docker compose exec -T analytics-db sh -lc 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -f /docker-entrypoint-initdb.d/01-schema.sql'
#
# ANALYTICS_DB_URL=postgresql://orchestrator:orchestrator@127.0.0.1:5432/orchestrator_analytics

# The Streamlit dashboard (`orchestrator/dashboard.py`) reads from
# the same ANALYTICS_DB_URL. It is opt-in via the `dashboard`
# dependency group so the default `uv sync --locked` keeps install
# minimal:
#   uv sync --group dashboard
#   uv run streamlit run orchestrator/dashboard.py
# An unset or `off` ANALYTICS_DB_URL renders a clear banner inside
# the app instead of crashing, so it is safe to launch the
# dashboard before deploying Postgres. The full end-to-end
# walkthrough -- start `analytics-db`, configure ANALYTICS_DB_URL,
# run `orchestrator.analytics.sync`, launch Streamlit, and
# interpret the in-app empty / error banners -- is in
# `docs/configuration.md` under "Analytics dashboard end-to-end".
#
# Secret-handling note: putting the Postgres password in
# ANALYTICS_DB_URL inside `.env` is acceptable because the URL is
# scoped to the local-only Postgres instance defined in
# `analytics-db/compose.yml` (port pinned to 127.0.0.1) and grants
# no GitHub access. This is intentionally different from
# GITHUB_TOKEN, which is kept out of `.env` because the
# sandbox-bypass implementer agent could read a `.env` placed in
# the worktree -- see the GITHUB_TOKEN section above for the
# rationale and the canonical token-file path.
